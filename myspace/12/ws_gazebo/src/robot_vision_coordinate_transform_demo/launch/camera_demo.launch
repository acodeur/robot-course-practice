<?xml version="1.0"?>
<launch>
  <!-- 第十二讲：静态相机场景。机器人不动，Gazebo 只提供相机图像与目标真值。 -->

  <include file="$(find gazebo_ros)/launch/empty_world.launch">
    <arg name="world_name" value="$(find robot_vision_coordinate_transform_demo)/gazebo/worlds/coordinate_transform_demo.world"/>
    <arg name="paused" value="false"/>
    <arg name="use_sim_time" value="true"/>
    <arg name="gui" value="true"/>
  </include>

  <node name="set_zero_gravity" pkg="rosservice" type="rosservice"
        args="call --wait /gazebo/set_physics_properties
          '{time_step: 0.001, max_update_rate: 1000.0,
            gravity: {x: 0.0, y: 0.0, z: 0.0},
            ode_config: {auto_disable_bodies: false,
              sor_pgs_precon_iters: 0, sor_pgs_iters: 50,
              sor_pgs_w: 1.3, sor_pgs_rms_error_tol: 0.0,
              contact_surface_layer: 0.001,
              contact_max_correcting_vel: 100.0,
              cfm: 0.0, erp: 0.2, max_contacts: 20}}'"
        output="screen"/>

  <param name="robot_description"
         textfile="$(find robot_vision_coordinate_transform_demo)/gazebo/urdf/humanoid_with_camera.urdf"/>

  <node name="robot_state_publisher" pkg="robot_state_publisher"
        type="robot_state_publisher" output="screen"/>

  <node name="spawn_model" pkg="gazebo_ros" type="spawn_model"
        args="-urdf -model humanoid -param robot_description"
        launch-prefix="bash -c 'sleep 2; $0 $@' "
        output="screen"/>
</launch>
